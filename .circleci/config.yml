version: 2.1
jobs:
  build:
    docker:
      - image: 'circleci/node:latest-browsers'
    steps:
      - checkout
      - run:
          name: "Install Dependancies"
          command: |
            npm install
      - run:
          name: "Run tests"
          command: |
            npm run test